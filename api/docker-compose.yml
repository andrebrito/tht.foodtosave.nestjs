version: '3'
services:
  database:
    container_name: foodtosave-nestjs-db
    image: postgres:14
    environment:
      - 'POSTGRES_DB=food-to-save-nestjs'
      - 'POSTGRES_USER=test'
      - 'POSTGRES_PASSWORD=test'
    ports:
      - '5430:5432'
